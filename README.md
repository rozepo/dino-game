# 🦕 Динозавр

Современная веб-игра в стиле Chrome Dino с монетками, магазином, апгрейдами, облачным сохранением прогресса через Supabase.
https://rozepo.github.io/dino-game/

## 🎮 Особенности

- **Монетки** - собирай монеты во время забега
- **Магазин** - покупай апгрейды прыжка (до 4 прыжков) и косметику
- **Горы** - новое препятствие, требующее минимум 2 прыжка
- **Облачное сохранение** - прогресс сохраняется в Supabase
- **Авторизация** - регистрация и вход через Supabase Auth
- **Гостевой режим** - можно играть без регистрации (localStorage)
- **Современный UI** - чистый дизайн в стиле Dodo Pizza
- **Мобильная оптимизация** - удобное управление на телефоне

## ⚙️ Настройка Supabase

### 1. Получи ключи Supabase

1. Зайди в свой проект на [supabase.com](https://supabase.com)
2. Перейди в **Settings** → **API**
3. Скопируй:
   - **Project URL** (SUPABASE_URL)
   - **anon/public key** (SUPABASE_ANON_KEY)

⚠️ **ВАЖНО**: Используй только **publishable/anon** ключ! **НЕ используй** secret/service_role ключи!

### 2. Установи ключи

Открой файл `supabaseClient.js` и замени плейсхолдеры:

```javascript
const SUPABASE_URL = 'PASTE_YOUR_SUPABASE_URL';  // ← Вставь свой URL
const SUPABASE_ANON_KEY = 'PASTE_YOUR_SUPABASE_ANON_KEY';  // ← Вставь свой anon key
```

### 3. Настрой базу данных

В Supabase SQL Editor выполни:

```sql
-- Создание таблицы player_state
CREATE TABLE IF NOT EXISTS public.player_state (
    user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
    coins INTEGER DEFAULT 0,
    high_score INTEGER DEFAULT 0,
    max_jumps INTEGER DEFAULT 1 CHECK (max_jumps >= 1 AND max_jumps <= 4),
    has_mask BOOLEAN DEFAULT false,
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Включение RLS
ALTER TABLE public.player_state ENABLE ROW LEVEL SECURITY;

-- Policy: пользователь может читать только свой профиль
CREATE POLICY "Users can read own profile"
ON public.player_state FOR SELECT
USING (auth.uid() = user_id);

-- Policy: пользователь может создавать свой профиль
CREATE POLICY "Users can insert own profile"
ON public.player_state FOR INSERT
WITH CHECK (auth.uid() = user_id);

-- Policy: пользователь может обновлять свой профиль
CREATE POLICY "Users can update own profile"
ON public.player_state FOR UPDATE
USING (auth.uid() = user_id);
```

## 🚀 Запуск

Просто открой `index.html` в браузере или используй локальный сервер:

```bash
# Python 3
python -m http.server 8000

# Или Node.js
npx http-server
```

Затем открой http://localhost:8000

**Примечание**: Проект работает на GitHub Pages без сборки. Все файлы загружаются напрямую через CDN.

## 🎯 Как играть

### Управление
- **ПРОБЕЛ** или **СТРЕЛКА ВВЕРХ** - прыжок (на десктопе)
- **Касание экрана** - прыжок (на мобильном)
- **P** или **ESC** - пауза

### Механика
- Избегай кактусов и гор
- Собирай монеты (появляются дугами)
- Горы требуют минимум 2 прыжка для прохождения
- Монеты сохраняются локально (гость) или в облаке (аккаунт)

## 🔐 Авторизация

### Гостевой режим
- Можно играть без регистрации
- Прогресс сохраняется в localStorage
- При входе можно перенести гостевой прогресс в аккаунт

### Облачный режим
- Регистрация через email/password
- Прогресс синхронизируется с Supabase
- Доступен с любого устройства после входа

## 🛒 Магазин

### Апгрейды прыжка
- **Двойной прыжок** (50 монет) - 2 прыжка в воздухе
- **Тройной прыжок** (150 монет) - 3 прыжка в воздухе
- **Четверной прыжок** (300 монет) - 4 прыжка в воздухе

### Косметика
- **Маска** (100 монет) - золотая маска для динозавра

## 📁 Структура проекта

```
dino_game/
├── index.html          # Главная страница с UI
├── style.css           # Стили в стиле Dodo Pizza
├── game.js             # Игровая логика
├── storage.js          # Модуль сохранения (localStorage + Supabase)
├── shop.js             # Модуль магазина
├── ui.js               # Модуль управления UI
├── auth.js             # Модуль авторизации через Supabase
├── supabaseClient.js   # Клиент Supabase (нужно вставить ключи)
└── README.md           # Документация
```

## 🔧 Модули

### storage.js
Управление сохранением:
- **Гостевой режим**: localStorage
- **Облачный режим**: Supabase (если залогинен)
- `init()` - инициализация (загружает облачные данные)
- `ensureLoaded()` - загрузка/создание облачного профиля
- `syncToCloud()` - синхронизация с дебаунсом (800мс)
- `forceSync()` - принудительная синхронизация
- `migrateGuestProgress()` - перенос гостевого прогресса в аккаунт

### auth.js
Авторизация через Supabase:
- `bootstrap()` - инициализация (получить сессию, подписаться на изменения)
- `signUp(email, pass)` - регистрация
- `signIn(email, pass)` - вход
- `signOut()` - выход
- `isLoggedIn()` - проверка авторизации
- `getEmail()` - получить email
- `onChange(callback)` - подписка на изменения авторизации

### shop.js
Магазин с товарами:
- `init()` - инициализация
- `render()` - отрисовка товаров
- `buy(itemId)` - покупка товара (с forceSync)

### ui.js
Управление экранами:
- `showScreen(screenId)` - показать экран
- `showHud()` / `hideHud()` - показать/скрыть HUD
- `updateScore()` / `updateCoins()` - обновление HUD
- `refreshHome()` - обновление Home экрана после auth событий
- `showNotification()` - уведомления

### game.js
Игровая логика:
- `Game.start()` - начало игры
- `Game.update()` - игровой цикл
- Классы: `Cactus`, `Coin`, `Mountain`
- Объект `dino` с поддержкой множественных прыжков

## 🎨 Дизайн

Дизайн вдохновлен Dodo Pizza:
- Чистые карточки с мягкими тенями
- Оранжевая цветовая схема (#FF6B35)
- Крупные кнопки (минимум 44px)
- Адаптивная верстка
- Safe-area для iPhone

## 📱 Мобильная оптимизация

- Полноэкранный режим
- Управление касанием
- Крупные кликабельные зоны
- Адаптивный HUD
- Поддержка safe-area

## 🔄 Обновление на GitHub Pages

После изменений:

```bash
git add .
git commit -m "feat: add supabase auth + cloud save"
git push
```

Подожди 1-2 минуты и обнови страницу с жесткой перезагрузкой (Cmd+Shift+R).

## 📝 Лицензия

Свободное использование
